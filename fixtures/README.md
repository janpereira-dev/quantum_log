# Fixtures

All files in this directory are synthetic test data. `session-a-b-a.ndjson` models one session switching from project A to B and back to A. It is not real activity and must never be used as reported usage.
