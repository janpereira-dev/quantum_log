# npm/Bun Template

This is deliberately private metadata, not a published npm package. Before publishing, reserve and verify `@janpereira.dev/quantum-log`, add platform binary packages or a checksum-verifying postinstall, and replace the development version. The published package must distribute the verified Go binary rather than reimplementing QUANTUM_LOG in JavaScript.
