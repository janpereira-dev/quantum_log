# Milestone 1 Evidence Matrix

## Current State

`M1` is `BLOCKED`. This matrix has no command output bound to the current candidate. Earlier release
notes and historical runs are retained elsewhere but are not promoted here.
Therefore no acceptance criterion is `PASS`.

`VERIFIED` requires every required row to be `PASS`. `FAIL`, `NOT_RUN`, and
`BLOCKED` prevent that transition. Source files, stubs, registrations, templates,
or isolated unit tests are not acceptance evidence by themselves.

| AC ID | Criterion | Test | Command | Platform | Candidate tag / commit | Result | Artifact path | Evidence | State |
|---|---|---|---|---|---|---|---|---|---|
| M1-01 | `go build ./...` succeeds. | Full build | `go build ./...` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-02 | `go test ./...` succeeds. | Full test suite | `go test -count=1 ./...` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-03 | Binary is named `qlog`. | Future test ID: `TestRootCommandName`. | `go test -count=1 ./internal/cli -run '^TestRootCommandName$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-04 | `qlog --version` shows version, commit, and build date. | Future test ID: `TestVersionOutputIncludesMetadata`. | `go test -count=1 ./internal/cli -run '^TestVersionOutputIncludesMetadata$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-05 | `qlog init` creates config and SQLite in OS-correct paths. | Future test ID: `TestInitCreatesPlatformPaths`. | `go test -count=1 ./internal/cli -run '^TestInitCreatesPlatformPaths$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-06 | `qlog init` is idempotent. | Future test ID: `TestInitIsIdempotent`. | `go test -count=1 ./internal/cli -run '^TestInitIsIdempotent$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-07 | `qlog doctor` does not modify the system. | Future test ID: `TestDoctorIsReadOnly`. | `go test -count=1 ./internal/cli -run '^TestDoctorIsReadOnly$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-08 | `qlog doctor --json` returns valid JSON. | Future test ID: `TestDoctorJSONOutput`. | `go test -count=1 ./internal/cli -run '^TestDoctorJSONOutput$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-09 | `qlog verify` accepts an intact chain. | Future test ID: `TestVerifyAcceptsIntactLedger`. | `go test -count=1 ./internal/cli -run '^TestVerifyAcceptsIntactLedger$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-10 | Manual event modification breaks verification. | Future test ID: `TestVerifyRejectsManualEventModification`. | `go test -count=1 ./internal/cli -run '^TestVerifyRejectsManualEventModification$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-11 | SQLite works with `CGO_ENABLED=0`. | CGo-free suite | `CGO_ENABLED=0 go test -count=1 ./...` | Linux/macOS shell; equivalent Windows environment run required | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-12 | Migrations are embedded. | Future test ID: `TestEmbeddedMigrations`. | `go test -count=1 ./internal/storage/sqlite -run '^TestEmbeddedMigrations$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-13 | Initial contracts/tables exist for required entities. | Future test ID: `TestInitialSchemaContracts`. | `go test -count=1 ./internal/storage/sqlite -run '^TestInitialSchemaContracts$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-14 | `project register --path .` is idempotent. | Future test ID: `TestProjectRegisterIsIdempotent`. | `go test -count=1 ./internal/cli -run '^TestProjectRegisterIsIdempotent$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-15 | `project current` shows project, location, method, and confidence. | Future test ID: `TestProjectCurrentHumanOutput`. | `go test -count=1 ./internal/cli -run '^TestProjectCurrentHumanOutput$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-16 | `project current --json` has stable JSON. | Future test ID: `TestProjectCurrentStableJSON`. | `go test -count=1 ./internal/cli -run '^TestProjectCurrentStableJSON$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-17 | Absolute path is not a permanent `Project` property. | Future test ID: `TestProjectDoesNotPersistAbsolutePath`. | `go test -count=1 ./internal/domain -run '^TestProjectDoesNotPersistAbsolutePath$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-18 | Git branch and commit belong to `WorkContext`. | Future test ID: `TestWorkContextOwnsGitMetadata`. | `go test -count=1 ./internal/domain -run '^TestWorkContextOwnsGitMetadata$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-19 | A session can move A -> B -> A without mixing usage. | Future test ID: `TestSessionProjectTransitionAtoBtoA`. | `go test -count=1 ./internal/cli -run '^TestSessionProjectTransitionAtoBtoA$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-20 | One identity can have multiple `ProjectLocation` values. | Future test ID: `TestProjectSupportsMultipleLocations`. | `go test -count=1 ./internal/storage/sqlite -run '^TestProjectSupportsMultipleLocations$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-21 | `--project` overrides `QLOG_PROJECT`. | Future test ID: `TestExplicitProjectOverridesEnvironment`. | `go test -count=1 ./internal/cli -run '^TestExplicitProjectOverridesEnvironment$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-22 | `QLOG_PROJECT` overrides CWD and Git inference. | Future test ID: `TestEnvironmentOverridesCWDAndGit`. | `go test -count=1 ./internal/cli -run '^TestEnvironmentOverridesCWDAndGit$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-23 | Impossible resolution remains `unattributed`. | Future test ID: `TestImpossibleResolutionIsUnattributed`. | `go test -count=1 ./internal/cli -run '^TestImpossibleResolutionIsUnattributed$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-24 | Events retain sanitized resolution method, confidence, and evidence. | Future test ID: `TestEventPersistsSanitizedResolutionEvidence`. | `go test -count=1 ./internal/storage/sqlite -run '^TestEventPersistsSanitizedResolutionEvidence$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-25 | Invalid allocation sums are rejected. | Future test ID: `TestInvalidAllocationSumRejected`. | `go test -count=1 ./internal/storage/sqlite -run '^TestInvalidAllocationSumRejected$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-26 | Prompt and response content are not stored. | Future test ID: `TestPromptAndResponseRedactedBeforePersistence`. | `go test -count=1 ./internal/storage/sqlite -run '^TestPromptAndResponseRedactedBeforePersistence$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-27 | README, LICENSE, SECURITY, CONTRIBUTING, and CHANGELOG exist. | Required tracked files: expect exit 0 only when all five paths match. | `git ls-files --error-unmatch README.md LICENSE SECURITY.md CONTRIBUTING.md CHANGELOG.md` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-28 | CI covers Linux, macOS, and Windows. | OS-matrix assertions: expect every platform query to exit 0. | `rg -q 'ubuntu' .github/workflows && rg -q 'macos' .github/workflows && rg -q 'windows' .github/workflows` | GitHub Actions workflow definition | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-29 | GoReleaser produces a local snapshot. | Snapshot assertion: expect release success and `dist` output. | `goreleaser release --snapshot --clean` | Release build host; archives target Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-30 | No secrets, binaries, or databases are committed. | Negative repository-hygiene assertion: return success only for a no-match result. | `go test -count=1 ./internal/distribution -run '^TestRepositoryHygiene' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-31 | Example data is marked DEMO or fixture. | Negative fixture-label assertion: return failure when any fixture lacks a marker. | `go test -count=1 ./internal/distribution -run '^TestFixtureLabels' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |
| M1-32 | Unexecuted build, test, or lint work is never reported as successful. | Public-claim audit | `go test -count=1 ./internal/distribution -run '^TestReleaseDocumentationDoesNotClaimUnsupportedEvidence$' -v` | Windows, macOS, Linux | Not bound — no current-candidate run | Not run for the current candidate. | None (`NOT_RUN`). | Historical notes, if any, are not promoted into this matrix. | `NOT_RUN` |

## Evidence Update Rules

- Keep one row per acceptance criterion from `QUANTUM_LOG_MASTER_PROMPT.md`.
- Bind every result to its exact command, platform, candidate tag/commit, and committed artifact path before changing a row to `PASS`.
- Use `FAIL` for executed checks that violate a criterion, `NOT_RUN` when no check has run, and `BLOCKED` when a prerequisite prevents execution.
- For `NOT_RUN` rows, name the future test but do not add a command that can pass without executing that test.
- Do not change `M1` from `BLOCKED` to `VERIFIED` until every required row is `PASS` and an independent review confirms the evidence.
