# M4 Closure Backlog

## Current baseline (2026-09-03)

The current published candidate is prerelease `v0.4.0-rc10`, commit `35ae43bd0031b3aca2621c52ede74731ae136357`. GitHub published its release archives, checksums, Sigstore bundle, and SBOMs on 2026-09-01. This establishes availability only. M4 remains `IN_PROGRESS`: no current-candidate two-machine or complete five-agent real-source acceptance is recorded.

The P0 ledger below is dated historical evidence. It must not be promoted to current-candidate `PASS` or `VERIFIED` evidence.

## Historical evidence: 2026-08-04

P0-01 audited `v0.3.2-rc.1`. At that date, it was the candidate supported by that audit's repository snapshot; it is not the current candidate.

## Historical Release Baseline

| Topic | Observed evidence |
|---|---|
| RC source of truth | `cmd/qlog/main.go` sets default version to `0.3.2`; `.goreleaser.yaml` injects the tag version with `-X main.version={{ .Version }}`. Candidate: `v0.3.2-rc.1`. |
| Branch and base | Current branch: `feat/m4-auto-capture` at `520acbf7ddb5e2b56fa8779be02bc3d2b613c305`. `origin/main` is an ancestor at `6ffdbaccdf5f381645168b56462cb9189c0ff89a`; `origin/main...HEAD` reports `0 45`. Current branch tracks `origin/feat/m4-auto-capture` and is ahead by 3 commits. |
| Tags | Local tags: `v0.1.0`, `v0.2.0`, `v0.3.0`, `v0.3.1`. No local `v0.3.2*` tag. |
| Release workflow | `.github/workflows/release.yml` releases on pushed `v*` tags through GoReleaser and uploads a Cosign checksum bundle. No release action was run. |
| Public metadata drift | `CHANGELOG.md` latest released section is `0.2.0`; `README.md` quick start installs `v0.3.0`; npm package and installer hard-code `0.1.0`. Homebrew, Scoop, AUR, and Winget templates use `{{VERSION}}`. |
| Working tree at audit start | Uncommitted modifications existed in `internal/cli/setup.go` and `internal/cli/setup_test.go`. They are outside P0-01 and remain unstaged. |

## Status Vocabulary

Allowed values: `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, `BLOCKED_EXTERNAL`, `COMPLETE`.

## P0 Ledger

| ID | Status | Change made | Validation command | Result | Evidence | Remaining blockers |
|---|---|---|---|---|---|---|
| P0-01 | COMPLETE | Created this audit ledger only. No product, installer, package, tag, remote, or workflow change. | `git status --short`; `git branch -vv`; `git log --oneline origin/main..HEAD`; `git tag --sort=-version:refname`; `git diff --check`; repository reads of release metadata. | Audit completed. Working tree had two pre-existing modified setup files; no local `v0.3.2*` tag; candidate resolves to `v0.3.2-rc.1`. | `cmd/qlog/main.go:11`; `.goreleaser.yaml:16-17`; `CHANGELOG.md:3`; `README.md:55`; `packaging/npm/package.json:3`; `packaging/npm/scripts/install.js:9-10`; `.github/workflows/release.yml:3-33`; `docs-int/verification/m4-evidence.md:7`; `internal/adapters/adapters_test.go:64`. | Runtime proof for Codex CLI and GitHub Copilot VS Code is not recorded. GitHub Copilot CLI is explicitly unavailable without separate official telemetry evidence and is absent from current stable adapter set. Release metadata versions remain inconsistent. |
| P0-02 | BLOCKED | Corrected shell and PowerShell candidate bootstrap paths: PowerShell now normalizes the install directory and passes the installed executable to setup; both installers run `doctor` after consented setup and report setup/health failures with recovery commands. Preserved explicit consent, opt-out, HTTPS-only download, SHA-256 verification, staged replacement, and no-path-modification behavior. | `go test -count=1 ./internal/distribution`; `pwsh -NoProfile -File installers/install.ps1 --dry-run --version v0.3.2-rc.1 --install-dir <temp> --no-modify-path --no-bootstrap`; local `go build -ldflags "-X main.version=v0.3.2-rc.1"`; built binary `setup --yes --dry-run --executable <absolute-path>`; built binary `init` then `doctor`; `C:\Program Files\Git\bin\sh.exe -n installers/install.sh`. | Focused installer tests passed. PowerShell dry-run resolved only `v0.3.2-rc.1` archive/checksum URLs and made no changes. Built candidate reported `qlog v0.3.2-rc.1`; setup dry-run produced plans only; isolated health check returned `doctor: ok`; shell syntax check passed. | `installers/install.sh`; `installers/install.ps1`; `internal/distribution/installers_test.go`; local command output captured in this execution. | Full installer download/bootstrap proof remains unavailable: `v0.3.2-rc.1` is intentionally non-public, and installers correctly reject non-HTTPS sources. Need a signed HTTPS RC artifact before performing an end-to-end installer invocation. Full `go test -count=1 ./...` is also blocked by pre-existing uncommitted setup executable validation that rejects transient test binaries. |
| P0-03 | COMPLETE | Pinned npm distributor metadata and both official installer defaults to candidate `v0.3.2-rc.1`; removed latest-release fallback so no omitted-version path silently selects `v0.1.0` or `v0.3.1`. GoReleaser snapshots now generate `0.3.2-rc.1`. Added explicit `QLOG_INSTALL_LOCAL_ARTIFACT_DIR` support for npm: it selects only exact host platform/architecture candidate archive plus `checksums.txt`, applies archive-size bound, SHA-256 verification, and existing safe extraction. Added generated-artifact package validation. Classifications: `PRODUCT_BUG` stale npm `0.1.0` downloader metadata and installer latest fallback; `TEST_OBSOLETO` npm URL/archive tests encoded `0.1.0`; `TEST_INFRASTRUCTURE` host npm 12 policy injects `allow-scripts=opencode-ai`, blocking automatic dependency lifecycle execution. Validation installs packed local package into isolated prefix with lifecycle scripts disabled, then invokes its installed declared `postinstall` against generated artifacts; this preserves host policy while exercising distributor code. | `goreleaser release --snapshot --clean`; `QLOG_INSTALL_LOCAL_ARTIFACT_DIR=<repo>/dist npm run test:artifact` from `packaging/npm`; `npm test` from `packaging/npm`; `go test -count=1 ./internal/distribution`; `pwsh -NoProfile -File installers/install.ps1 --dry-run --install-dir <temp> --no-modify-path --no-bootstrap`; `sh -n installers/install.sh`. | GoReleaser produced six `0.3.2-rc.1` platform archives and `checksums.txt`. Generated-artifact test packed and installed local npm package, verified its installed `postinstall` downloaded no network artifact, verified exact local checksum/archive, and executed installed `qlog 0.3.2-rc.1`. npm unit suite passed (6 pass, generated-artifact case skipped unless explicit artifact path supplied). Focused distribution tests passed. PowerShell dry-run resolved only `v0.3.2-rc.1` archive/checksum URLs without writes. | `.goreleaser.yaml:39-40`; `packaging/npm/package.json:3,29-34`; `packaging/npm/scripts/install.js`; `packaging/npm/tests/install.test.js`; `packaging/npm/README.md`; `installers/install.sh`; `installers/install.ps1`; generated `dist/qlog_0.3.2-rc.1_*` archives and `dist/checksums.txt`. | `TEST_INFRASTRUCTURE`: host npm 12 policy prevents automatic `postinstall` in project-scoped test installs; test retains policy and invokes installed declared script explicitly. `REGRESSION`: full `go test -count=1 ./...` remains blocked by pre-existing unstaged setup executable-validation failures in `internal/cli/setup.go` and `internal/cli/setup_test.go`, outside P0-03. |
| P0-04 | COMPLETE | Enforced durable executable validation for `setup`: reject Go `.test` and `go-build` paths before generated hooks/config can retain them. Classification: durable-path rejection is `PRODUCT_CONTRACT`; six affected setup tests are `TEST_FIXTURE` because they exercise setup behavior, not executable durability, and now supply a temporary durable executable. Added black-box built-artifact setup coverage for Claude Code hook configuration. | `go test -count=1 ./internal/cli -run Setup`; `go test -count=1 ./internal/cli`; `go test -count=1 ./...`; `go vet ./...`. | CLI setup tests pass, including built-artifact setup: generated hook references built `qlog.exe` and contains neither `go-build` nor `.test`. Full suite and vet pass. | `internal/cli/setup.go:195-214`; `internal/cli/collector.go:293-299`; `internal/cli/setup_test.go`; `internal/cli/capture_commands_test.go`; command output captured in this execution. | None. |
| P0-05 | COMPLETE | Validated the compiled `v0.3.2-rc.1` artifact in isolated `QLOG_HOME`: `setup --yes` initialized the ledger, detected and configured only installed Claude Code, Codex, Copilot VS Code, and OpenCode integrations, and printed each first-event `qlog adapter verify <adapter>` command. Fixed `PRODUCT_BUG`: Windows Task Scheduler external-policy denial previously aborted setup before integration configuration or health evidence. Setup now records the exact scheduler diagnostic, probes collector health, then continues supported detected integrations. `TEST_OBSOLETO`: setup coverage lacked external-policy continuation; added focused regression coverage. No `TEST_INFRASTRUCTURE`, `REGRESSION`, or `NON_BLOCKING` issue observed. No external agent events were generated. | `go test -count=1 ./internal/cli`; `go test -count=1 ./...`; `go vet ./...`; `go build -o <temp>/qlog.exe -ldflags "-X main.version=v0.3.2-rc.1" ./cmd/qlog`; isolated artifact `adapter detect --json`; isolated artifact `setup --yes`; isolated artifact `doctor --json`; isolated artifact `collector status --json`; isolated artifact `adapter status --json`. | All Go tests and vet passed. Artifact detected/configured four installed stable integrations and created isolated `qlog.db`; `doctor` returned `status=ok`. Windows Task Scheduler returned `Error: Acceso denegado.` for collector `/Create`; setup preserved exact diagnosis and reported `/healthz` connection refusal. | `internal/cli/setup.go`; `internal/cli/setup_test.go`; runtime artifact output captured in this execution. | Collector cannot activate on this Windows host until Task Scheduler permits creation of `QUANTUM_LOG Collector`; health remains unreachable. First-event verification intentionally remains pending real agent use; do not inject synthetic external events. |
| P0-06 | BLOCKED_EXTERNAL | Ran only compiled `qlog v0.3.2-rc.1` artifact at `C:\Users\cowbo\AppData\Local\Temp\opencode\p0-05\install\qlog.exe` with isolated `QLOG_HOME=C:\Users\cowbo\AppData\Local\Temp\opencode\p0-06-20260804`. `setup --yes --json` configured Codex OTLP with `log_user_prompt = false`. Managed collector creation was denied by Windows Task Scheduler, so documented local fallback `qlog collector serve --home <isolated-home> --log-file <isolated-home>\collector.log` ran foreground on `127.0.0.1:4318`; no managed restart PASS claimed. One real authenticated `codex exec --sandbox read-only --cd <repo> "Reply with exactly: P0-06 Codex acceptance."` action returned the requested text and reported `tokens used 6202`. No synthetic OTLP or event injection occurred. | `qlog --version`; `go version -m <artifact>`; `codex --version`; `codex login status`; isolated `setup --yes --json`; `collector status --json`; `doctor --json`; one `codex exec`; `adapter verify codex --since 10m --json`; `usage today --json`; `export --format json --from 2026-08-04 --to 2026-08-05`; `verify`. | Artifact reports `qlog v0.3.2-rc.1`; Codex CLI `0.146.0` is authenticated with ChatGPT; setup settings, collector health, and isolated database health passed. Foreground collector reported `installed=false`, `running=true`, `reachable=true`, `health=ok`. Codex action completed, but no OTLP request was persisted: adapter verification failed only `raw_evidence`; usage returned `total_tokens=0`; export returned `[]`; no event or model-call IDs exist. `qlog verify` returned `ledger: verified`. Replay behavior cannot be exercised without a persisted real event; `qlog replay` is not a command. Sanitized persistence cannot be proven without a received event; configuration disables user-prompt logging. | Exact managed-collector policy condition: `task scheduler operation /Create for task "QUANTUM_LOG Collector" failed: exit status 1: Error: Acceso denegado.` Exact capture blocker after real action: Codex `0.146.0` emitted no observable OTLP payload to configured healthy `http://127.0.0.1:4318/v1/logs` during the sole permitted action. No product-code defect established; do not run a second action or inject a test event. |
| P0-07 | BLOCKED_EXTERNAL | Ran only compiled `qlog v0.3.2-rc.1` artifact at `C:\Users\cowbo\AppData\Local\Temp\opencode\p0-05\install\qlog.exe` with isolated `QLOG_HOME=C:\Users\cowbo\AppData\Local\Temp\opencode\p0-07-20260804`. VS Code `1.131.0` is installed, but `code --list-extensions --show-versions` lists only `ms-ceintl.vscode-language-pack-es@1.131.2026072717`, not GitHub Copilot. `setup --yes --json` wrote only qlog-managed Copilot OTel settings with `captureContent=false`; no real Copilot Chat/Agent action was attempted because extension and login surface are absent. No synthetic OTLP or event injection occurred. | `qlog --version`; `code --version`; `code --list-extensions --show-versions`; isolated `adapter detect --json`; isolated `setup --yes --json`; `collector status --json`; `adapter status copilot-vscode --json`; `adapter verify copilot-vscode --since 10m --json`; `usage today --json`; `export --format json --from 2026-08-04 --to 2026-08-05`; `verify`; `doctor --json`. | Artifact reports `qlog v0.3.2-rc.1`; VS Code detection and qlog-managed privacy settings passed. Task Scheduler denied collector creation; collector was unreachable. `adapter verify` passed settings, VS Code availability, capture quality, and source evidence, but failed collector and raw evidence. `usage today` returned zero rows/tokens and export returned `[]`; `verify` returned `ledger: verified`; `doctor` returned `status=ok`. Replay and persisted-payload privacy inspection cannot run without a source-originated event. | `%APPDATA%\Code\User\settings.json:83-87` records `github.copilot.chat.otel.captureContent=false`, loopback exporter endpoint, and qlog ownership; isolated `qlog.db` remained event-free. | Install and authenticate GitHub Copilot extension in VS Code, start a supported loopback collector (Task Scheduler currently returns `task scheduler operation /Create for task "QUANTUM_LOG Collector" failed: exit status 1: Error: Acceso denegado.`), then make one real Copilot Chat/Agent action. Run `adapter verify copilot-vscode --since 10m --json`, `usage project <slug> --json`, repeat documented upstream replay, inspect sanitized persisted/exported payloads, then rerun `verify` and `doctor`. |
| P0-08 | BLOCKED_EXTERNAL | Added stable `copilot` adapter using official GitHub Copilot CLI user hooks. `qlog setup copilot --yes` writes only qlog-owned `~/.copilot/hooks/qlog.json`; it does not modify user settings, project hooks, prompts, tool data, or credentials. The hook parser accepts only session ID and CWD, emits local `lifecycle_only` evidence, and delegates project ownership to the central resolver. Source/reported tokens remain unavailable. | TDD RED/GREEN: `go test -count=1 ./internal/adapters ./internal/cli ./internal/ingest/qlogevent`; runtime: built `qlog v0.3.2-rc.1`, isolated `QLOG_HOME`, `COPILOT_HOME`, and `QLOG_ADAPTER_CONFIG_HOME`; `setup copilot --yes --json`; one `copilot -p` action; `adapter status/test/verify`; usage/export; `verify`; `doctor --json`; `adapter uninstall copilot --json`. | Focused tests passed. Copilot CLI `1.0.73` was detected and authenticated; one real action returned `P0-08 Copilot acceptance.` Hook config installed and adapter status/test passed. No raw event reached isolated qlog ledger, so verify failed `collector` and `raw_evidence`; usage/export remained empty. Ledger verification and doctor passed. Cleanup removed only qlog's hook file and status then showed `installed=false`. No synthetic agent event was created. | Official sources: https://docs.github.com/en/copilot/reference/hooks-reference, https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-plugin-reference; `internal/adapters/copilot_cli.go`; `internal/cli/hook.go`; `internal/ingest/qlogevent/handler.go`; isolated artifact/runtime output under `C:\Users\cowbo\AppData\Local\Temp\opencode\p0-08`. | Determine why Copilot CLI 1.0.73 did not invoke or deliver user hooks for this non-interactive authenticated action, then repeat with one real source-originated event. Inspect replay and persisted-payload privacy only after a real event exists. |
| P0-09 | BLOCKED_EXTERNAL | Added and executed `scripts/smoke-v0.3.2-rc.1-windows.ps1`, a Windows-capable black-box harness that accepts only generated `qlog_0.3.2-rc.1_windows_amd64.zip` with pinned SHA-256, extracts to an isolated temporary installation, and never invokes a repository executable or internal API. It initializes isolated `QLOG_HOME` and adapter-config state, registers a clean project, detects adapters, runs setup twice, diagnoses managed-collector policy, starts only documented foreground fallback, records expected empty-ledger verification/query/export paths, writes real-agent instructions, and supports `-PostAgent` automatic restart/persistence/query inspection. Classification: `TEST_INFRASTRUCTURE` for Windows Task Scheduler policy (`/Create` denied); `PRODUCT_BUG` for candidate setup aborting before adapter configuration on that known policy condition, so it lacks P0-05 continuation behavior; `NON_BLOCKING` for expected no-agent `adapter verify` failures and zero usage/export. No telemetry was simulated. | `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\smoke-v0.3.2-rc.1-windows.ps1`; `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\smoke-v0.3.2-rc.1-windows.ps1 -EvidenceRoot C:\Users\cowbo\AppData\Local\Temp\qlog-p0-09-20260804-221440 -PostAgent`. | Both runs exit `23` as precise `BLOCKED_EXTERNAL_POLICY`. Archive SHA-256 matched `206c812a8237dbafb4705fddbe93e9e17fd0e4ca80fa3e17f900dcb785c4fde5`; extracted artifact reported `qlog 0.3.2-rc.1`. Both setup attempts exited `1` with `task scheduler operation /Create for task "QUANTUM_LOG Collector" failed: exit status 1: Error: Acceso denegado.` Foreground fallback became reachable. Detection/status, zero-event adapter verification, project/daily usage, export, post-agent-negative restart/status, `verify`, and `doctor` ran with no synthetic input. | `scripts/smoke-v0.3.2-rc.1-windows.ps1`; `C:\Users\cowbo\AppData\Local\Temp\qlog-p0-09-20260804-221440\evidence\summary.json`; `...\evidence\NEXT-REAL-AGENT-ACTION.txt`; `...\logs\*.log`. | Rebuild generated `v0.3.2-rc.1` Windows artifact after P0-05 so setup continues configuring adapters when Task Scheduler policy denies managed collector. Then rerun harness and complete one real detected-agent action followed by `-PostAgent`; no injected telemetry is permitted. |
| P0-10 | BLOCKED_EXTERNAL | Executed package/install/setup validation from generated `v0.3.2-rc.1` artifacts only. Strongest clean-equivalent available was a new Docker `node:22-alpine` container: no repository mount, host `qlog`, `QLOG_HOME`, ledger, collector, profile, adapters, or agent binaries. Transferred only packed npm package, exact Linux archive, and `checksums.txt`; npm `postinstall` selected local artifact and verified its SHA-256 before installing. Ran the applicable Windows P0-09 artifact-only harness separately after pinning its checksum to this snapshot's Windows archive. No agent events were fabricated. Validation level: `APPROXIMATE`, not clean-device acceptance. | `goreleaser release --snapshot --clean`; `npm.cmd pack --json --pack-destination <temp>`; `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\smoke-v0.3.2-rc.1-windows.ps1 -ArtifactPath dist\qlog_0.3.2-rc.1_windows_amd64.zip`; fresh container: `npm install --prefix /clean/install <package>` with `QLOG_INSTALL_LOCAL_ARTIFACT_DIR=/artifacts`; installed `qlog --version`; `init`; `project register`; `adapter detect --json`; `setup --yes --json`; `collector status --json`; `adapter status --json`; `usage project p0-10 --json`; `verify`; `doctor --json`. | Snapshot generated six candidate archives. npm installed exactly one package from local packed artifact and installed `qlog 0.3.2-rc.1` (`6178c6b`, built `2026-08-04T20:35:34Z`); isolated init/project registration passed; all adapters correctly reported unavailable; empty usage returned zero rows/tokens; `verify` returned `ledger: verified`; `doctor` returned `status=ok`. Package `setup --yes --json` stopped at `exec: "systemctl": executable file not found in $PATH`; collector state was installed but stopped/unreachable. Windows P0-09 harness accepted the regenerated SHA-256-pinned archive and exited `23` as `BLOCKED_EXTERNAL_POLICY` after Task Scheduler denied managed collector creation; foreground fallback succeeded. | `dist/checksums.txt` (`f8189e03d1338c7e1b7e9f5970a9a21c9df2616aff3f6cbf75510ce29d2a460f` Linux amd64; `1e343d21c71ef78fd32a7694c8a30d7031e30596e6bfded2991d917501ee14e3` Windows amd64); `scripts/smoke-v0.3.2-rc.1-windows.ps1`; Windows harness evidence `C:\Users\cowbo\AppData\Local\Temp\qlog-p0-09-20260804-224036`; fresh container `qlog-p0-10-clean` (`56c67cf359e8aeac5aa3503c5939942c07229f2d0c53fe007112b8a7492b637b`). | Windows Sandbox is unavailable and feature-state inspection requires elevation; no clean VM/new local user is available. Docker Alpine lacks `systemctl`, so managed collector setup cannot pass there. Clean image contains no supported agent, so real source-originated capture, replay, and persisted-payload privacy inspection remain unproven. Run same artifact on a clean Windows VM/new user with Task Scheduler permission and one installed authenticated supported agent; do not inject telemetry. |
| P0-11 | COMPLETE | Prepared local-only `v0.3.2-rc.1` snapshot from clean ephemeral clone at exact HEAD `dba6ca4040b93b889ead41ec90d4b2ffd19226c1`; no tag, remote, npm publish, or push. GoReleaser 2.17.1 produced six archives, six archive SBOMs, `checksums.txt`, `artifacts.json`, and `metadata.json`. `GOFLAGS=-buildvcs=false` was required because GoReleaser's ignored `dist` output otherwise made Go embed a false `vcs.modified=true`; configured linker flags still embed exact version and commit. Snapshot config has no signing stage and local Cosign is unavailable, so no signature bundle was produced. | `docker run --rm --entrypoint sh -e "GOFLAGS=-buildvcs=false" -v "C:\\Users\\cowbo\\Repositorios\\quantum_log:/source:ro" -v "C:\\Users\\cowbo\\AppData\\Local\\Temp\\opencode\\qlog-p0-11-dist:/out" goreleaser/goreleaser:latest -c "set -e; rm -rf /work /out/*; git clone file:///source /work; cd /work; git checkout --detach dba6ca4040b93b889ead41ec90d4b2ffd19226c1; git diff --exit-code; goreleaser release --snapshot --clean; cp -a dist/. /out/"`; manifest SHA-256 verification over all entries plus archive-manifest/metadata checks; extracted `dist/qlog_0.3.2-rc.1_windows_amd64.zip` then `qlog.exe --version`; `QLOG_INSTALL_LOCAL_ARTIFACT_DIR=<repo>\\dist npm.cmd run test:artifact`; `powershell -NoProfile -ExecutionPolicy Bypass -File installers\\install.ps1 --dry-run --version v0.3.2-rc.1 --install-dir C:\\Users\\cowbo\\AppData\\Local\\Temp\\opencode\\qlog-p0-11-installer --no-modify-path --no-bootstrap`. | Build, checksum/SBOM/artifact manifest consistency, embedded version/commit, package black-box installation, and installer URL/reference checks passed. Extracted binary: `qlog 0.3.2-rc.1 (commit dba6ca4040b93b889ead41ec90d4b2ffd19226c1, built 2026-08-04T20:43:16Z)`. | `dist/qlog_0.3.2-rc.1_darwin_amd64.tar.gz` `66196f4ba2430682c2ca8f8d35869b5456fc8008d811b73ceaa2f2c5785b5259`; `dist/qlog_0.3.2-rc.1_darwin_amd64.tar.gz.sbom.json` `fb8bdbb04508a85f4cfa0cf3a81aa73d5408c45cc567ddb97be62e7a1fad83ce`; `dist/qlog_0.3.2-rc.1_darwin_arm64.tar.gz` `d3a2eb7e6fc930e5409a2fae93db81472ab8b728d760ecf7a4c486453d54c344`; `dist/qlog_0.3.2-rc.1_darwin_arm64.tar.gz.sbom.json` `5a120c904946b063331dacd755ec64c0e31092644036345461bc1e7a0a39f800`; `dist/qlog_0.3.2-rc.1_linux_amd64.tar.gz` `f34c24ac8725cd4d5936d3445927df0348172107ec90aef145b8fe93ae029554`; `dist/qlog_0.3.2-rc.1_linux_amd64.tar.gz.sbom.json` `bc9f32856118b602caab7ccf84b864bb22edfc5726f2d289052c80ba9c6ff13e`; `dist/qlog_0.3.2-rc.1_linux_arm64.tar.gz` `5c6158129a1bc451cc061a51794d7143f086cbb00e25d927935661b29a4e5505`; `dist/qlog_0.3.2-rc.1_linux_arm64.tar.gz.sbom.json` `ce0a493b8293b6522037a492795b9be61c4899a54fea3fea05c0ab964649af35`; `dist/qlog_0.3.2-rc.1_windows_amd64.zip` `ff3eeebe1f95d8fc06f640e573b9480010e333b6fb4657d3b535020877bcc64d`; `dist/qlog_0.3.2-rc.1_windows_amd64.zip.sbom.json` `eb1ccc683c6d6a4b861122614259b4ad4576b8f7dd1604bad7283b00fdd3ea82`; `dist/qlog_0.3.2-rc.1_windows_arm64.zip` `b09f6c660a08c4fb3dc2a0f08ffc2a776473e73aea083a247f30679bc9af45d6`; `dist/qlog_0.3.2-rc.1_windows_arm64.zip.sbom.json` `696179819b653bf20a081f73b0b37c2bb6d6cb2f2681571978b174a54940b975`; manifests: `dist/checksums.txt`, `dist/artifacts.json`, `dist/metadata.json`. | Detached local snapshot only. No Sigstore bundle: release workflow signs only after non-snapshot release and no local Cosign signer/tool is configured. Generated `dist/` remains ignored and is intentionally excluded from commit. |
| P0-12 | COMPLETE | Added user-facing installation, auto-capture, verification, and troubleshooting guides grounded only in P0-03 through P0-11 observed RC behavior; updated root README links and support status. Documentation distinguishes one-command published install from local RC artifact validation, records Windows Scheduler foreground fallback, privacy boundaries, cleanup, and current adapter matrix. | Extracted compiled P0-11 artifact `qlog_0.3.2-rc.1_windows_amd64.zip`: `--version`, `adapter list --json`; isolated-home `init`, `project register`, `project current --json`, `adapter detect --json`, `setup --dry-run --json`, `collector status --json`, `adapter status --json`, `usage project`, `export --format json`, `verify`, `doctor --json`; `git diff --check`. | Artifact reported `qlog 0.3.2-rc.1` with commit `dba6ca4040b93b889ead41ec90d4b2ffd19226c1`; all listed isolated local commands completed with expected empty-ledger status. `adapter verify` is intentionally non-zero before a real source event, and `setup --yes`/real agent actions remain described only from P0 evidence rather than rerun against user configuration. No real capture PASS is claimed. | `README.md`; `docs/INSTALL.md`; `docs/AUTOCAPTURE.md`; `docs/VERIFY.md`; `docs/TROUBLESHOOTING.md`; P0-05 through P0-11 rows and `docs-int/verification/m4-evidence.md`. | External real-capture acceptance remains blocked or guided as documented: Codex no observed OTLP request, Copilot CLI no delivered hook event, Copilot VS Code extension/login absent, Windows managed Scheduler policy denied. |

## P0-02 Test Issue Classification

| Classification | Issue | Disposition |
|---|---|---|
| PRODUCT_BUG | PowerShell bootstrap called `setup --yes` without the verified installed executable, so generated integration configuration could not retain a durable executable path. Neither installer executed `doctor` after consented setup. | Fixed in both official installers. |
| TEST_OBSOLETO | Existing installer text tests accepted any `qlog setup --yes` reference and did not require the PowerShell durable path or post-bootstrap health check. | Added targeted installer contract coverage. |
| TEST_INFRASTRUCTURE | End-to-end installer download cannot use a local HTTP fixture because HTTPS-only source validation is required; candidate `v0.3.2-rc.1` is not public, and P0-02 must not fall back to an older GitHub release. | Keep HTTPS and checksum controls unchanged. Block end-to-end proof until signed HTTPS RC artifact exists. |
| REGRESSION | Full `go test -count=1 ./...` fails six existing CLI setup tests because pre-existing unstaged `internal/cli/setup.go` calls `validateCollectorExecutable` for the transient `go test` binary. P0-02 does not modify either setup file. | Out of scope. Fix its test injection or setup behavior separately; focused distribution tests pass. |
| NON_BLOCKING | Initial ad-hoc PowerShell parser command escaped `$?` incorrectly and failed outside product code. Subsequent PowerShell `--dry-run` executed the script successfully, while Git `sh -n` validated shell syntax. | No product change required. |

## P0-06 Codex Diagnosis

**Status: `BLOCKED_EXTERNAL`.** No QLog defect was proven. Codex CLI `0.146.0` accepted the installed user-level `[otel]` log exporter under `--strict-config`; `codex doctor --json` reported `config.load=ok`, ChatGPT authentication, and healthy provider reachability. The real `codex exec` session persisted its response and token usage locally, but the healthy foreground QLog collector received no request: its only log line was its listener startup.

| Boundary | Evidence | Result |
|---|---|---|
| Codex configuration | `%USERPROFILE%\.codex\config.toml` contains `exporter = { otlp-http = { endpoint = "http://127.0.0.1:4318/v1/logs", protocol = "binary" } }` and `log_user_prompt = false`; `qlog-otel-state.json` owns these keys. | Accepted by Codex. No unowned configuration was mutated. |
| Signal and endpoint | Current official Codex source defines `exporter` for logs independently of `trace_exporter`; current QLog collector serves protobuf and JSON logs at `/v1/logs`. | Configured log signal and endpoint match. Adding `trace_exporter` cannot cause Codex log export. |
| Codex runtime | `~/.codex/sessions/2026/08/04/rollout-2026-08-04T20-14-47-019fcdfc-3c4c-77d2-a103-ffc487824bfa.jsonl` records the authenticated `codex_exec` action, exact response, and `17210` total tokens. | Action executed; local session recording is not an OTLP export. |
| Collector and decoder | Foreground collector startup recorded `qlog collector listening on http://127.0.0.1:4318 (/v1/traces and /v1/logs OTLP JSON/protobuf, /v1/events qlog JSON)`; previous live status reported `running=true`, `reachable=true`, `health=ok`; no request or decoder error followed. | Failure occurred before QLog request handling, so collector decoding cannot be causal. |

Reproduce without synthetic telemetry:

```powershell
$home = 'C:\Users\cowbo\AppData\Local\Temp\opencode\p0-06-20260804'
$qlog = 'C:\Users\cowbo\AppData\Local\Temp\opencode\p0-05\install\qlog.exe'
$env:QLOG_HOME = $home
& $qlog collector serve --home $home --log-file "$home\collector.log"
# Separate terminal, while collector remains healthy:
codex --strict-config exec --sandbox read-only --cd C:\Users\cowbo\Repositorios\quantum_log "Reply with exactly: P0-06 Codex acceptance."
& $qlog --home $home adapter verify codex --since 10m --json
Get-Content "$home\collector.log"
```

Expected current result: Codex action completes, `raw_evidence` remains false, and collector log contains no export request. Do not add synthetic events or alter Codex trace configuration to claim acceptance.

## P0-06 Test Issue Classification

| Classification | Issue | Disposition |
|---|---|---|
| NON_BLOCKING | Unit tests exercise QLog's supported OTLP log decoder but cannot substitute a real Codex emission. | Keep tests as decoder coverage only; do not treat them as runtime proof. |
| TEST_INFRASTRUCTURE | None observed. Foreground collector was healthy during real-action evidence collection. | No change. |
| PRODUCT_BUG | None proven. Config parsing, signal selection, endpoint exposure, and decoder boundary all passed. | No product change. |
